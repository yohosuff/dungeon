{
  "name": "dungeon",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf out && tsc",
    "serve": "cd out && node --experimental-specifier-resolution=node index.js",
    "start": "npm run build && npm run serve"
  },
  "license": "MIT",
  "dependencies": {
    "socket.io": "^4.3.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  }
}
