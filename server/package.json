{
  "name": "dungeon",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf out && tsc",
    "start": "npm run build && cd out && node index.js",
    "troubleshoot": "tsc --init"
  },
  "license": "MIT",
  "dependencies": {
    "socket.io": "^4.3.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  }
}
